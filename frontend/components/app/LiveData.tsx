"use client";

import Link from "next/link";
import { useCallback, useEffect, useMemo, useState } from "react";
import { ArrowRight, CalendarDays, Check, Download, FileText, Plus, Play, Radar as RadarIcon, RefreshCw, Search, UploadCloud, UserRound, X } from "lucide-react";
import { PageHeader } from "./PageHeader";
import { EmptyState, MetricCard, RegisterList, StatusPill } from "./Ui";
import { serviceIaFetch } from "@/lib/service-ia/api";
import type { Metric, RegisterItem } from "@/lib/service-ia/types";
import { RegisterCreateDialog } from "./RegisterCreateDialog";
import { Dialog, FormError } from "./Dialog";

type ApiPage<T> = { items: T[]; total: number; page: number; page_size: number };
type Offer = { id:string; name:string; category?:string|null; status:string; price?:string|null; currency:string; updated_at:string };
type Sale = { id:string; reference:string; client_name?:string|null; payment_status:string; total_amount:string; currency:string; sale_date:string };
type Procedure = { id:string; title:string; department?:string|null; status:string; version:number; next_review_date?:string|null };
type Alert = { id:string; title:string; explanation:string; priority:string; dimension:string; status:string; confidence:number };
type Action = { id:string; title:string; status:string; priority:string; responsible_user_id?:string|null; due_date?:string|null };
type Validation = { id:string; field_name:string; old_value:unknown; proposed_value:unknown; source_type:string; confidence:number; status:string };
type Organization = { id:string; name:string; slug:string; is_active:boolean };
type Member = { id:string; user_id:string; role:string; status:string };
type Attachment = { id:string; filename:string; register_type:string; record_id:string; size_bytes:number; created_at:string };
type Rule = { id:string; rule_code:string; enabled:boolean; priority:string; parameters:Record<string,unknown> };
type Invitation = { id:string; email:string; role:string; status:string; expires_at:string };

function label(value: unknown) {
  if (value === null || value === undefined || value === "") return "—";
  return String(value).replaceAll("_", " ");
}
function money(value: string | null | undefined, currency: string) {
  if (value == null) return "Sur devis";
  const number = Number(value);
  return Number.isFinite(number) ? `${new Intl.NumberFormat("fr-FR").format(number)} ${currency}` : `${value} ${currency}`;
}
function date(value?: string | null) {
  return value ? new Intl.DateTimeFormat("fr-FR").format(new Date(value)) : "—";
}

function useApi<T>(path: string) {
  const [data,setData]=useState<T|null>(null); const [error,setError]=useState(""); const [loading,setLoading]=useState(true);
  const reload=useCallback(async()=>{setLoading(true);setError("");try{setData(await serviceIaFetch<T>(path));}catch(e){setError(e instanceof Error?e.message:"API indisponible");}finally{setLoading(false)}},[path]);
  useEffect(()=>{void reload()},[reload]); return {data,error,loading,reload};
}
function State({loading,error,empty}:{loading:boolean;error:string;empty:boolean}) {
  if(loading)return <EmptyState title="Chargement…" detail="Connexion aux données Service IA."/>;
  if(error)return <EmptyState title="Données indisponibles" detail={error}/>;
  if(empty)return <EmptyState title="Aucune donnée" detail="Aucun élément réel n’est encore enregistré pour cette organisation."/>;
  return null;
}

export function LiveRegister({kind}:{kind:"offers"|"sales"|"procedures"}) {
  const config={offers:["Offres & tarifs","Conservez un tarif officiel, ses conditions et sa période de validité."],sales:["Ventes","Suivez les ventes, les paiements et les informations à compléter."],procedures:["Procédures","Formalisez les méthodes de travail, responsables et dates de révision."]}[kind];
  const api=useApi<ApiPage<Offer|Sale|Procedure>>(`/registers/${kind}`);
  const [creating,setCreating]=useState(false); const [query,setQuery]=useState("");
  const items=useMemo<RegisterItem[]>(()=>api.data?.items.map(item=>{
    if(kind==="offers"){const x=item as Offer;return{id:x.id,title:x.name,subtitle:x.category||"Sans catégorie",status:label(x.status),meta:`Mis à jour le ${date(x.updated_at)}`,value:money(x.price,x.currency)}}
    if(kind==="sales"){const x=item as Sale;return{id:x.id,title:x.reference,subtitle:x.client_name||"Client non renseigné",status:label(x.payment_status),meta:date(x.sale_date),value:money(x.total_amount,x.currency)}}
    const x=item as Procedure;return{id:x.id,title:x.title,subtitle:x.department||"Sans département",status:label(x.status),meta:`Version ${x.version} · Révision ${date(x.next_review_date)}`};
  })??[],[api.data,kind]);
  const filtered=items.filter(item=>`${item.title} ${item.subtitle}`.toLowerCase().includes(query.toLowerCase()));
  return <><PageHeader eyebrow="Registre" title={config[0]} description={config[1]} action={<button className="app-button app-button-primary" onClick={()=>setCreating(true)}><Plus size={16}/>Ajouter</button>}/><section className="app-panel"><div className="app-toolbar"><label className="app-search"><Search size={17}/><input value={query} onChange={event=>setQuery(event.target.value)} placeholder="Rechercher…"/></label><button className="app-button app-button-secondary" onClick={()=>void api.reload()}><RefreshCw size={15}/>Actualiser</button></div><State loading={api.loading} error={api.error} empty={!items.length}/>{filtered.length?<RegisterList items={filtered}/>:null}{items.length&&!filtered.length?<EmptyState title="Aucun résultat" detail="Modifiez votre recherche."/>:null}</section><RegisterCreateDialog kind={kind} open={creating} onClose={()=>setCreating(false)} onCreated={api.reload}/></>;
}

export function LiveDashboard(){
  const alerts=useApi<Alert[]>("/radar/alerts"); const actions=useApi<Action[]>("/workflow/actions");
  const offers=useApi<ApiPage<Offer>>("/registers/offers?page_size=1"); const sales=useApi<ApiPage<Sale>>("/registers/sales?page_size=1"); const procedures=useApi<ApiPage<Procedure>>("/registers/procedures?page_size=1");
  const open=(alerts.data??[]).filter(x=>x.status!=="resolved"&&x.status!=="ignored"); const active=(actions.data??[]).filter(x=>x.status!=="completed"&&x.status!=="ignored");
  const metrics:Metric[]=[{label:"Alertes ouvertes",value:String(open.length),detail:"Résultats Radar réels",tone:open.length?"warning":"positive"},{label:"Actions actives",value:String(active.length),detail:"Workflow opérationnel",tone:"neutral"},{label:"Offres",value:String(offers.data?.total??0),detail:"Registre réel",tone:"neutral"},{label:"Ventes",value:String(sales.data?.total??0),detail:`${procedures.data?.total??0} procédures`,tone:"neutral"}];
  const error=alerts.error||actions.error||offers.error||sales.error||procedures.error; const loading=alerts.loading||actions.loading||offers.loading||sales.loading||procedures.loading;
  return <><PageHeader eyebrow="Vue d’ensemble" title="Votre entreprise devient plus lisible." description="Données opérationnelles chargées depuis Service IA."/><State loading={loading} error={error} empty={false}/>{!loading&&!error?<><section className="app-metrics">{metrics.map(m=><MetricCard key={m.label} metric={m}/>)}</section><section className="app-grid-two"><article className="app-panel"><div className="app-panel-head"><h2>Alertes à traiter</h2><Link href="/espace/radar">Voir Radar <ArrowRight size={15}/></Link></div>{open.slice(0,5).map(a=><div className="app-alert" key={a.id}><RadarIcon size={18}/><div><strong>{a.title}</strong><p>{a.explanation}</p></div><StatusPill>{label(a.priority)}</StatusPill></div>)}{!open.length?<EmptyState title="Aucune alerte" detail="Lancez Radar pour analyser vos registres."/>:null}</article><article className="app-panel"><div className="app-panel-head"><h2>Actions en cours</h2><Link href="/espace/actions">Tout voir <ArrowRight size={15}/></Link></div>{active.slice(0,5).map(a=><div className="app-action" key={a.id}><div><strong>{a.title}</strong><span>{a.responsible_user_id||"Non assignée"}</span></div><StatusPill>{label(a.status)}</StatusPill></div>)}{!active.length?<EmptyState title="Aucune action" detail="Les actions créées depuis Radar apparaîtront ici."/>:null}</article></section></>:null}</>;
}

export function LiveActions(){
  const q=useApi<Action[]>("/workflow/actions"); const [creating,setCreating]=useState(false); const [error,setError]=useState(""); const [saving,setSaving]=useState(false);
  const create=async(event:React.FormEvent<HTMLFormElement>)=>{event.preventDefault();setSaving(true);setError("");const data=new FormData(event.currentTarget);try{await serviceIaFetch("/workflow/actions",{method:"POST",body:JSON.stringify({title:String(data.get("title")||""),description:String(data.get("description")||"")||null,priority:String(data.get("priority")||"normal"),responsible_user_id:String(data.get("responsible_user_id")||"")||null,due_date:String(data.get("due_date")||"")||null})});await q.reload();setCreating(false)}catch(e){setError(e instanceof Error?e.message:"Création impossible")}finally{setSaving(false)}};
  const move=async(action:Action,status:string)=>{await serviceIaFetch(`/workflow/actions/${action.id}`,{method:"PATCH",body:JSON.stringify({status})});await q.reload()};
  return <><PageHeader eyebrow="Exécution" title="Actions correctives" description="Créez, assignez et faites progresser les actions issues de Radar." action={<button className="app-button app-button-primary" onClick={()=>setCreating(true)}><Plus size={16}/>Nouvelle action</button>}/><State loading={q.loading} error={q.error} empty={!q.data?.length}/>{q.data?.length?<section className="app-kanban">{["todo","in_progress","blocked","completed"].map(status=><div className="app-kanban-column" key={status}><div className="app-kanban-head"><strong>{label(status)}</strong><span>{q.data?.filter(a=>a.status===status).length}</span></div>{q.data?.filter(a=>a.status===status).map(a=><article className="app-task" key={a.id}><StatusPill>{label(a.priority)}</StatusPill><h3>{a.title}</h3><div><span><UserRound size={14}/>{a.responsible_user_id||"Non assignée"}</span><span><CalendarDays size={14}/>{date(a.due_date)}</span></div><div className="app-row-actions">{status!=="in_progress"&&status!=="completed"?<button className="app-text-button" onClick={()=>void move(a,"in_progress")}>Démarrer</button>:null}{status!=="completed"?<button className="app-text-button" onClick={()=>void move(a,"completed")}>Terminer</button>:null}</div></article>)}</div>)}</section>:null}<Dialog open={creating} onClose={()=>setCreating(false)} title="Créer une action"><form onSubmit={create}><div className="app-form-grid"><label className="app-form-span">Titre *<input name="title" required minLength={2}/></label><label>Priorité<select name="priority"><option value="low">Faible</option><option value="normal">Normale</option><option value="high">Haute</option><option value="critical">Critique</option></select></label><label>Échéance<input name="due_date" type="date"/></label><label className="app-form-span">Responsable (identifiant KORYXA)<input name="responsible_user_id"/></label><label className="app-form-span">Description<textarea name="description"/></label></div><FormError>{error}</FormError><div className="app-form-actions"><button type="button" className="app-button app-button-secondary" onClick={()=>setCreating(false)}>Annuler</button><button className="app-button app-button-primary" disabled={saving}>{saving?"Création…":"Créer"}</button></div></form></Dialog></>}

export function LiveRadar(){const q=useApi<Alert[]>("/radar/alerts");const [running,setRunning]=useState(false);const run=async()=>{setRunning(true);try{await serviceIaFetch("/radar/runs",{method:"POST"});await q.reload()}finally{setRunning(false)}};const update=async(id:string,status:string)=>{await serviceIaFetch(`/radar/alerts/${id}`,{method:"PATCH",body:JSON.stringify({status})});await q.reload()};const action=async(a:Alert)=>{await serviceIaFetch("/workflow/actions",{method:"POST",body:JSON.stringify({alert_id:a.id,title:a.title,description:a.explanation,priority:a.priority})});await update(a.id,"acknowledged")};return <><PageHeader eyebrow="Qualité des données" title="Knowlia Radar" description="Analysez chaque alerte, créez une action ou marquez-la comme traitée." action={<button className="app-button app-button-primary" disabled={running} onClick={run}><Play size={16}/>{running?"Analyse…":"Lancer Radar"}</button>}/><State loading={q.loading} error={q.error} empty={!q.data?.length}/>{q.data?.length?<section className="app-panel"><div className="app-alert-list">{q.data.map(a=><article className="app-alert app-alert-large" key={a.id}><RadarIcon size={18}/><div><strong>{a.title}</strong><p>{a.explanation}</p><div className="app-alert-tags"><span>{label(a.dimension)}</span><span>{label(a.status)}</span></div></div><StatusPill>{label(a.priority)}</StatusPill><div className="app-row-actions">{a.status!=="resolved"?<button className="app-button app-button-secondary" onClick={()=>void action(a)}>Créer une action</button>:null}{a.status!=="resolved"?<button className="app-text-button" onClick={()=>void update(a.id,"resolved")}>Résoudre</button>:null}</div></article>)}</div></section>:null}</>}

export function LiveValidations(){const q=useApi<Validation[]>("/workflow/validations?status=pending");const decide=async(id:string,decision:"accepted"|"rejected")=>{await serviceIaFetch(`/workflow/validations/${id}/decision`,{method:"POST",body:JSON.stringify({decision,justification:"Décision depuis l’espace Service IA"})});await q.reload()};return <><PageHeader eyebrow="Contrôle humain" title="Validations" description="Propositions réelles en attente de décision."/><State loading={q.loading} error={q.error} empty={!q.data?.length}/>{q.data?.length?<section className="app-panel"><div className="app-validation-list">{q.data.map(v=><article className="app-validation" key={v.id}><div className="app-validation-head"><div><strong>{v.field_name}</strong><span>{label(v.source_type)}</span></div><StatusPill>{Math.round(v.confidence*100)}%</StatusPill></div><div className="app-change"><div><small>Valeur actuelle</small><strong>{label(v.old_value)}</strong></div><span>→</span><div><small>Valeur proposée</small><strong>{label(v.proposed_value)}</strong></div></div><div className="app-validation-actions"><button className="app-button app-button-secondary" onClick={()=>void decide(v.id,"rejected")}><X size={15}/>Rejeter</button><button className="app-button app-button-primary" onClick={()=>void decide(v.id,"accepted")}><Check size={15}/>Accepter</button></div></article>)}</div></section>:null}</>}

export function LiveOrganization(){const org=useApi<Organization>("/organizations/current");const members=useApi<Member[]>("/members");const invitations=useApi<Invitation[]>("/invitations");const [inviting,setInviting]=useState(false);const [error,setError]=useState("");const invite=async(event:React.FormEvent<HTMLFormElement>)=>{event.preventDefault();setError("");const data=new FormData(event.currentTarget);try{await serviceIaFetch("/invitations",{method:"POST",body:JSON.stringify({email:String(data.get("email")||""),role:String(data.get("role")||"contributor")})});await invitations.reload();setInviting(false)}catch(e){setError(e instanceof Error?e.message:"Invitation impossible")}};const role=async(member:Member,value:string)=>{await serviceIaFetch(`/members/${member.id}/role`,{method:"PATCH",body:JSON.stringify({role:value})});await members.reload()};return <><PageHeader eyebrow="Équipe" title="Organisation & membres" description="Gérez les membres, leurs rôles et les invitations." action={<button className="app-button app-button-primary" onClick={()=>setInviting(true)}><Plus size={16}/>Inviter</button>}/><State loading={org.loading||members.loading} error={org.error||members.error} empty={false}/>{org.data&&members.data?<><section className="app-panel"><div className="app-panel-head"><div><span className="app-eyebrow">Organisation</span><h2>{org.data.name}</h2></div><StatusPill>{members.data.length} membres</StatusPill></div><div className="app-list">{members.data.map(m=><div className="app-list-row" key={m.id}><div className="app-list-icon"><UserRound size={18}/></div><div className="app-list-main"><strong>{m.user_id}</strong><span>{label(m.status)}</span></div><select value={m.role} onChange={event=>void role(m,event.target.value)} aria-label={`Rôle de ${m.user_id}`}><option value="owner">Propriétaire</option><option value="manager">Responsable</option><option value="contributor">Contributeur</option><option value="viewer">Lecteur</option></select></div>)}</div></section>{invitations.data?.length?<section className="app-panel"><div className="app-panel-head"><h2>Invitations</h2><StatusPill>{invitations.data.length}</StatusPill></div><div className="app-list">{invitations.data.map(i=><div className="app-list-row" key={i.id}><div className="app-list-main"><strong>{i.email}</strong><span>{label(i.role)} · expire le {date(i.expires_at)}</span></div><StatusPill>{label(i.status)}</StatusPill></div>)}</div></section>:null}</>:null}<Dialog open={inviting} onClose={()=>setInviting(false)} title="Inviter un membre"><form onSubmit={invite}><div className="app-form-grid"><label className="app-form-span">Adresse e-mail *<input name="email" type="email" required/></label><label className="app-form-span">Rôle<select name="role"><option value="contributor">Contributeur</option><option value="manager">Responsable</option><option value="viewer">Lecteur</option></select></label></div><FormError>{error}</FormError><div className="app-form-actions"><button type="button" className="app-button app-button-secondary" onClick={()=>setInviting(false)}>Annuler</button><button className="app-button app-button-primary">Envoyer l’invitation</button></div></form></Dialog></>}

export function LiveDocuments(){const [registerType,setRegisterType]=useState("offer");const [recordId,setRecordId]=useState("");const [items,setItems]=useState<Attachment[]>([]);const [error,setError]=useState("");const [loading,setLoading]=useState(false);const load=async()=>{if(!recordId.trim())return;setLoading(true);setError("");try{setItems(await serviceIaFetch(`/imports/attachments?register_type=${encodeURIComponent(registerType)}&record_id=${encodeURIComponent(recordId.trim())}`))}catch(e){setError(e instanceof Error?e.message:"Documents indisponibles")}finally{setLoading(false)}};const upload=async(event:React.ChangeEvent<HTMLInputElement>)=>{const file=event.target.files?.[0];if(!file||!recordId.trim())return;const body=new FormData();body.set("file",file);body.set("register_type",registerType);body.set("record_id",recordId.trim());setLoading(true);setError("");try{await serviceIaFetch("/imports/attachments",{method:"POST",body});await load()}catch(e){setError(e instanceof Error?e.message:"Ajout impossible")}finally{setLoading(false);event.target.value=""}};return <><PageHeader eyebrow="Mémoire documentaire" title="Documents & Knowlia" description="Ajoutez et consultez les justificatifs associés aux registres."/><section className="app-panel"><div className="app-toolbar"><select value={registerType} onChange={e=>setRegisterType(e.target.value)}><option value="offer">Offre</option><option value="sale">Vente</option><option value="procedure">Procédure</option></select><input value={recordId} onChange={e=>setRecordId(e.target.value)} placeholder="Identifiant de l’élément"/><button className="app-button app-button-secondary" onClick={()=>void load()}>Charger</button><label className="app-button app-button-primary">Ajouter un fichier<input className="sr-only" type="file" onChange={upload} disabled={!recordId.trim()}/></label></div><p className="app-panel-note">Sélectionnez le registre et renseignez l’identifiant de l’élément avant d’ajouter un document.</p><State loading={loading} error={error} empty={!loading&&!error&&!items.length}/>{items.length?<div className="app-list">{items.map(d=><article className="app-list-row" key={d.id}><div className="app-list-icon"><FileText size={19}/></div><div className="app-list-main"><strong>{d.filename}</strong><span>{d.register_type} · {Math.ceil(d.size_bytes/1024)} Ko</span></div><StatusPill>{date(d.created_at)}</StatusPill></article>)}</div>:null}</section></>}

export function LiveSettings(){const q=useApi<Rule[]>("/radar/rules");const update=async(rule:Rule,patch:Record<string,unknown>)=>{await serviceIaFetch(`/radar/rules/${rule.rule_code}`,{method:"PUT",body:JSON.stringify({enabled:rule.enabled,priority:rule.priority,parameters:rule.parameters,...patch})});await q.reload()};return <><PageHeader eyebrow="Configuration" title="Paramètres Radar" description="Activez les règles et adaptez leur priorité."/><State loading={q.loading} error={q.error} empty={!q.data?.length}/>{q.data?.length?<section className="app-panel"><div className="app-list">{q.data.map(r=><article className="app-list-row" key={r.id}><div className="app-list-main"><strong>{r.rule_code}</strong><span>{Object.keys(r.parameters).length} paramètres</span></div><select value={r.priority} onChange={event=>void update(r,{priority:event.target.value})} aria-label={`Priorité de ${r.rule_code}`}><option value="low">Faible</option><option value="normal">Normale</option><option value="high">Haute</option><option value="critical">Critique</option></select><button className={`app-button ${r.enabled?"app-button-secondary":"app-button-primary"}`} onClick={()=>void update(r,{enabled:!r.enabled})}>{r.enabled?"Désactiver":"Activer"}</button></article>)}</div></section>:null}</>}

export function LiveImports(){const [file,setFile]=useState<File|null>(null);const [registerType,setRegisterType]=useState("offers");const [preview,setPreview]=useState<{id:string;row_count:number;suggested_mapping:Record<string,string>;errors:unknown[]}|null>(null);const [error,setError]=useState("");const [busy,setBusy]=useState(false);const send=async()=>{if(!file)return;setError("");setBusy(true);const body=new FormData();body.set("file",file);body.set("register_type",registerType);try{setPreview(await serviceIaFetch("/imports/preview",{method:"POST",body}))}catch(e){setError(e instanceof Error?e.message:"Import impossible")}finally{setBusy(false)}};const confirm=async()=>{if(!preview)return;setBusy(true);setError("");try{await serviceIaFetch(`/imports/${preview.id}/confirm`,{method:"POST",body:JSON.stringify({column_mapping:preview.suggested_mapping})});setPreview(null);setFile(null)}catch(e){setError(e instanceof Error?e.message:"Confirmation impossible")}finally{setBusy(false)}};const exportCsv=()=>{window.location.href=`/api/service-ia/imports/export/${registerType}`};return <><PageHeader eyebrow="Migration" title="Importer vos données" description="Prévisualisez, contrôlez puis confirmez un fichier CSV ou XLSX." action={<button className="app-button app-button-secondary" onClick={exportCsv}><Download size={16}/>Exporter CSV</button>}/><section className="app-panel app-upload"><label>Registre<select value={registerType} onChange={e=>{setRegisterType(e.target.value);setPreview(null)}}><option value="offers">Offres</option><option value="sales">Ventes</option><option value="procedures">Procédures</option></select></label><label className="app-dropzone"><input type="file" accept=".csv,.xlsx" onChange={e=>{setFile(e.target.files?.[0]??null);setPreview(null)}}/><UploadCloud size={34}/><strong>{file?file.name:"Déposez votre fichier ici"}</strong><span>CSV ou XLSX · 10 Mo maximum</span></label><FormError>{error}</FormError>{file&&!preview?<button className="app-button app-button-primary" disabled={busy} onClick={()=>void send()}>{busy?"Analyse…":"Prévisualiser"}</button>:null}{preview?<div className="app-import-preview"><h2>{preview.row_count} lignes détectées</h2><StatusPill>{preview.errors.length} erreurs</StatusPill>{Object.entries(preview.suggested_mapping).map(([from,to])=><div className="app-mapping" key={from}><span>{from} → {to}</span></div>)}<button className="app-button app-button-primary" disabled={busy||preview.errors.length>0} onClick={()=>void confirm()}>{busy?"Import…":"Confirmer l’import"}</button></div>:null}</section></>}
