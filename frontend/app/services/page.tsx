import type { Metadata } from "next";
import Link from "next/link";
import { ArrowRight } from "lucide-react";
import { PremiumCta, SectionHeading } from "@/components/marketing/ServiceMarketing";
import { ServiceCard } from "@/components/services/ServiceCards";
import { getPillarServices, servicePillars } from "@/lib/services/catalog";
export const metadata: Metadata={title:"Services | KORYXA Service IA & Web",description:"Explorez les services web, IA, applications, automatisation, data et accompagnement KORYXA."};
export default function ServicesPage(){return <div className="space-y-20"><section className="pt-8 sm:pt-14"><SectionHeading eyebrow="Catalogue" title="Un catalogue complet, organisé pour rester simple." text="Choisissez un pôle, puis le service qui correspond à votre besoin. Chaque offre dispose de sa propre page détaillée."/></section>{servicePillars.map((p,index)=>{const services=getPillarServices(p.slug);return <section id={p.slug} key={p.slug} className="scroll-mt-28 border-t border-slate-200 pt-12"><div className="flex flex-col gap-5 lg:flex-row lg:items-end lg:justify-between"><div className="max-w-2xl"><span className="text-xs font-bold uppercase tracking-[.2em] text-cyan-700">Pôle 0{index+1}</span><h2 className="mt-3 text-3xl font-semibold tracking-[-.04em] text-slate-950 sm:text-4xl">{p.title}</h2><p className="mt-3 leading-7 text-slate-600">{p.description}</p></div><Link href={`/services/${p.slug}`} className="inline-flex items-center gap-2 text-sm font-bold text-cyan-700">Voir le pôle <ArrowRight className="h-4 w-4"/></Link></div><div className="mt-8 grid gap-4 sm:grid-cols-2 xl:grid-cols-3">{services.map(s=><ServiceCard key={s.slug} service={s}/>)}</div></section>})}<PremiumCta/></div>}
