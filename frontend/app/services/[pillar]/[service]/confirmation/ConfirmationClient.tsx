"use client";
import Link from "next/link";import { useEffect,useState } from "react";import { CheckCircle2,Copy } from "lucide-react";
type Payload={reference:string;serviceTitle:string;pillarTitle:string;company:string;name:string;email:string;timeline:string;budget:string};
export default function ConfirmationClient({pillar,service}:{pillar:string;service:string}){const [data,setData]=useState<Payload|null>(null);useEffect(()=>{try{const raw=sessionStorage.getItem("koryxa:last-request");if(raw)setData(JSON.parse(raw));}catch{}},[]);const ref=data?.reference||"KX-DEMANDE";return <section className="mx-auto max-w-3xl pt-8 sm:pt-14"><div className="rounded-[2rem] border border-emerald-200 bg-white p-6 shadow-[0_25px_70px_rgba(15,23,42,.08)] sm:p-10"><span className="flex h-14 w-14 items-center justify-center rounded-2xl bg-emerald-100 text-emerald-700"><CheckCircle2 className="h-7 w-7"/></span><p className="mt-6 text-xs font-bold uppercase tracking-[.2em] text-emerald-700">Demande préparée</p><h1 className="mt-3 text-3xl font-semibold tracking-[-.045em] text-slate-950 sm:text-4xl">Merci, votre demande est prête pour le cadrage.</h1><p className="mt-4 leading-7 text-slate-600">{data?`Votre demande « ${data.serviceTitle} » pour ${data.company} a bien été préparée.`:"Votre demande a bien été préparée."} L’équipe KORYXA pourra reprendre le contexte du service sans vous faire recommencer.</p><div className="mt-7 rounded-2xl bg-white p-5 text-[var(--kx-text)]"><p className="text-xs font-bold uppercase tracking-[.18em] text-emerald-600">Référence</p><div className="mt-2 flex items-center justify-between gap-4"><strong className="text-xl tracking-wide">{ref}</strong><button onClick={()=>navigator.clipboard.writeText(ref)} aria-label="Copier la référence" className="rounded-xl border border-white/15 p-2 text-emerald-600"><Copy className="h-4 w-4"/></button></div></div><div className="mt-7 flex flex-col gap-3 sm:flex-row"><Link href={`/services/${pillar}/${service}`} className="inline-flex min-h-12 items-center justify-center rounded-full bg-white px-6 font-bold text-[var(--kx-text)]">Retour au service</Link><Link href="/services" className="inline-flex min-h-12 items-center justify-center rounded-full border border-slate-300 px-6 font-bold text-slate-700">Explorer les services</Link></div></div></section>}
