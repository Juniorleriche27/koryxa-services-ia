import type { Metadata } from "next";import { notFound } from "next/navigation";import { getService } from "@/lib/services/catalog";import ConfirmationClient from "./ConfirmationClient";export const metadata:Metadata={title:"Confirmation de demande | KORYXA"};export default async function Page({params}:{params:Promise<{pillar:string;service:string}>}){const x=await params;if(!getService(x.pillar,x.service))notFound();return <ConfirmationClient pillar={x.pillar} service={x.service}/>}
