import { Play, Radar as RadarIcon } from "lucide-react"; import { PageHeader } from "@/components/app/PageHeader"; import { MetricCard, PrimaryButton, StatusPill } from "@/components/app/Ui"; import { alerts, metrics } from "@/lib/service-ia/demo";
export default function Page(){return <><PageHeader eyebrow="Qualité des données" title="Knowlia Radar" description="Des règles explicables pour repérer les informations manquantes, anciennes ou contradictoires." action={<PrimaryButton><Play size={16}/>Lancer Radar</PrimaryButton>}/><section className="app-metrics">{metrics.map(m=><MetricCard key={m.label} metric={m}/>)}</section><section className="app-panel"><div className="app-panel-head"><div><span className="app-eyebrow">Alertes</span><h2>Résultats de la dernière analyse</h2></div><span>Il y a 8 minutes</span></div><div className="app-alert-list">{alerts.map(a=><article className="app-alert app-alert-large" key={a.id}><div className="app-alert-icon"><RadarIcon size={18}/></div><div><strong>{a.title}</strong><p>{a.explanation}</p><div className="app-alert-tags"><span>{a.dimension}</span><span>{a.status}</span></div></div><StatusPill>{a.priority}</StatusPill><button className="app-button app-button-secondary">Créer une action</button></article>)}</div></section></>}
