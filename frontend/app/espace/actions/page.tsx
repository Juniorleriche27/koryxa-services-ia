import { CalendarDays, Plus, UserRound } from "lucide-react"; import { PageHeader } from "@/components/app/PageHeader"; import { PrimaryButton, StatusPill } from "@/components/app/Ui"; import { actions } from "@/lib/service-ia/demo";
export default function Page(){return <><PageHeader eyebrow="Exécution" title="Actions correctives" description="Transformez les alertes en responsabilités claires avec une échéance et une preuve de résolution." action={<PrimaryButton><Plus size={16}/>Nouvelle action</PrimaryButton>}/><section className="app-kanban">{['À faire','En cours','Bloquée'].map(column=><div className="app-kanban-column" key={column}><div className="app-kanban-head"><strong>{column}</strong><span>{actions.filter(a=>a.status===column).length}</span></div>{actions.filter(a=>a.status===column).map(a=><article className="app-task" key={a.id}><StatusPill>{a.priority}</StatusPill><h3>{a.title}</h3><div><span><UserRound size={14}/>{a.responsible_user_id}</span><span><CalendarDays size={14}/>{a.due_date}</span></div></article>)}</div>)}</section></>}
